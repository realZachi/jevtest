{
  "name": "jevtest-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update": "vitest run -u",
    "demo": "tsx scripts/showcase.ts",
    "demo:fail": "JEVTEST_DEMO_FAIL=1 vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jevtest": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
